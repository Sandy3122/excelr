/**
 * Admin Settings (close registrations at a date/time).
 *
 * Held: /admin/settings and the settings API return 404.
 * To enable, comment the `true` line and uncomment the `false` line.
 */
export const HOLD_ADMIN_SETTINGS = false;
// export const HOLD_ADMIN_SETTINGS = false;
